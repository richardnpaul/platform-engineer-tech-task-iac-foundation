apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app-ui
  namespace: demo-app
  labels:
    app: demo-app-ui
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: demo-app-ui
  template:
    metadata:
      labels:
        app: demo-app-ui
    spec:
      containers:
        - name: demo-app-ui
          image: 793421532223.dkr.ecr.eu-west-1.amazonaws.com/dev-app-repo:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 10m
              memory: 128Mi
            limits:
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
